# VPS-Fortress

✅ **VPS Security Enterprise – محافظ سرور شما با SSH امن، UFW، Fail2ban و IP Blocking**  

VPS-Fortress یک اسکریپت Bash برای افزایش امنیت سرورهای لینوکسی است که شامل ویژگی‌های زیر می‌باشد:

- تغییر پورت SSH با پیش‌فرض امن `(Default 2222)`  
- غیرفعال کردن ورود root به SSH  
- فعال‌سازی **UFW** با قوانین پیش‌فرض  
- فعال‌سازی **Fail2ban** و جلوگیری از حملات Brute-force  
- افزودن IP های **Cloudflare/CDN** به لیست بلاک  
- پشتیبان‌گیری خودکار از فایل SSH قبل از اعمال تغییرات  
- قابلیت rollback خودکار در صورت بروز خطا در SSH  

---

## ⚡ نصب و اجرا

برای اجرای اسکریپت با یک دستور کافیست:

```bash
curl -O https://raw.githubusercontent.com/erfanesmizadh/VPS-Fortress/main/install.sh
chmod +x install.sh
sudo bash install.sh
```



در هنگام اجرا، اسکریپت از شما سه ورودی می‌پرسد:
User ports (Default 2100,2200,8080,8880)
پورت‌هایی که مربوط به کاربران هستند. اگر Enter بزنید، مقدار پیش‌فرض استفاده می‌شود.
Traffic ports (Default 80,443,8080)
پورت‌هایی که می‌خواهید ترافیک از آنها عبور کند. Enter برای استفاده از پیش‌فرض.
New SSH port (Default 2222)
پورت جدید برای SSH سرور. Enter = پیش‌فرض 2222
⚠️ قبل از logout از ترمینال فعلی، مطمئن شوید اتصال SSH به پورت جدید موفق است تا از lockout جلوگیری شود.
📄 ویژگی‌ها
✅ SSH امن با پورت جدید
✅ Root login غیرفعال
✅ UFW با قوانین پیش‌فرض و پورت‌های موردنظر باز
✅ Fail2ban فعال و محافظت در برابر حملات
✅ ipset برای بلاک کردن IP های مخرب
✅ افزودن Cloudflare/CDN IP ها به لیست بلاک
✅ پشتیبان‌گیری خودکار از SSH و امکان rollback
🛠️ دستورات وضعیت
بررسی وضعیت UFW:
Copy code
Bash
sudo ufw status verbose
بررسی وضعیت Fail2ban:
Copy code
Bash
sudo fail2ban-client status
مشاهده IP های بلاک شده:
Copy code
Bash
sudo ipset list banned
بازگردانی SSH در صورت مشکل:
Copy code
Bash
sudo cp /etc/ssh/sshd_config.bak /etc/ssh/sshd_config
sudo systemctl restart sshd
🔒 نکات امنیتی
قبل از تغییر پورت SSH، اسکریپت تست config و port انجام می‌دهد
اگر SSH روی پورت جدید fail شود، اسکریپت به صورت خودکار revert می‌کند
همه پیش‌فرض‌ها قابل تغییر هستند ولی توصیه می‌شود از مقادیر امن استفاده کنید
💻 پیش‌نیازها
سیستم‌عامل Ubuntu / Debian
دسترسی root یا sudo
